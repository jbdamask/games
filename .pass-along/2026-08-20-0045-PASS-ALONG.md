---
DATE: 2026-08-20T00:45:52+0000
TITLE: Surf Rats clock, waves, retrieves, box, and trophy are live — feel untested
STATUS: in_progress
MODEL: unknown
MODEL_EFFORT: unknown
HARNESS: cursor
SESSION_ID: eb4d41bc-ac84-4d19-a9c1-8fae1c3ff86c
REPO: jbdamask/games
WORKING_DIR: /home/rocky
BRANCH: main
HEAD_SHA: 66440d3
UPSTREAM: origin/main (ahead 0)
WORKTREE: dirty
ISSUE_TRACKER: GitHub
OPEN_LOOPS:
  - "New wave shove / pointy-peak knock-off, lure techniques, snag rates, and trophy case have not been playtested"
  - "Map marker coordinates still estimated, not verified against Wetzel labels on montauk-map.jpg"
  - "Durable clone of jbdamask/games is only at /tmp/games — /tmp is ephemeral"
  - "Local /home/rocky/surf-rats/ is a scratch copy, not a git repo"
  - "Pass-along chain is uncommitted (staged after this file is added)"
  - "Bite / weed / hike time still paper-tuned; lure technique multipliers are new and unfelt"
RECOMMENDED_NEXT: Play the live game focusing on rock waves and bucktail snags — those two rates were set without a playthrough and can make the new systems feel broken.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-19-2348-PASS-ALONG.md
PRIME: ["open https://johndamask.com/games/surf-rats/", "git -C /tmp/games status || gh repo clone jbdamask/games"]
---

## Start here

**Surf Rats is live** at https://johndamask.com/games/surf-rats/ in `jbdamask/games` (`66440d3`). The 6-second trip bug is fixed (2 real minutes = 1 game hour). Waves, lure retrieves, a limited tackle box, and a trophy case shipped this session. Nobody has played that build.

Predecessor: `.pass-along/2026-08-19-2348-PASS-ALONG.md` (game was live at `b405d0d`, unplaytested). Clone: `/tmp/games`. Cursor workspace: `/home/rocky` (not a git repo). Source of truth: `surf-rats/index.html`.

## May have moved since I stopped

- **Pages build for `jbdamask/games`.** Last checked 00:45 UTC: status `built`, commit `66440d3`. Recheck `gh api repos/jbdamask/games/pages/builds/latest` and `curl -sL -A "Mozilla/5.0" https://johndamask.com/games/surf-rats/ | grep STARTER_BOX` if the page looks old (CDN `max-age=600`).
- **`/tmp/games`** is still the only local clone. If this machine rebooted or `/tmp` was wiped, `gh repo clone jbdamask/games`.
- **`jbdamask.github.io`** was last left at `79df5af` (revert of a mistaken `/games/` add). Only recheck if `/games/` on the blog looks wrong — that path is served by `jbdamask/games`.
- No CI, PRs, or background jobs besides GitHub Pages.

## What this session did

Cursor session `eb4d41bc`. Started with `/session-recap` off the 23:48 pass-along, then the user played the live game.

- **Clock bug, not a bad instruction.** `dt` is real seconds. `G.elapsedMin += dt * 60` made one real second = one game hour, so a 6-hour trip died in ~6 seconds. User asked for **two real minutes = one game hour**. Commit `74dddac`. Constants: `REAL_SECONDS_PER_GAME_HOUR = 120`, `GAME_MIN_PER_REAL_SEC` (~line 446). Trip length is 12 real minutes.
- **Gameplay pass** after the user called it cool and asked for four things. Commit `66440d3`, Pages built. All in `surf-rats/index.html`:
  - Waves shove shoreward; knock-off only if seaward stance or `peak === "point"` (`shoveFromRock` ~1074, spawn rate cut in `updateSurf`).
  - Retrieves: popper tap-SPACE shake, darter reel + UP every other turn, minnow hold-SPACE slow, bucktail low/slow + UP lifts (`updateLureWork` ~1100). Wrong retrieve lowers `techMul` on bite chance.
  - Box `STARTER_BOX` (~460): 3 bucktails, 2 poppers, 2 darters, 2 minnows. Snag → `loseLure`.
  - Trophy case: `localStorage` key `surfrats-trophies`. Title / CASE / trip-over.
- Scratch copy `/home/rocky/surf-rats/index.html` was copied to match after the push.

## Commits

Since predecessor `HEAD_SHA` `b405d0d`, both **pushed** to `origin/main`:

- `74dddac` Slow the Surf Rats clock to two real minutes per game hour.
- `66440d3` Make Surf Rats fish like Montauk: shove waves, real retrieves, loseable plugs.

`jbdamask.github.io` was not touched this session.

## Uncommitted work

`.pass-along/` in `/tmp/games` (this file plus `2026-08-19-2327` and `2026-08-19-2348`). Finished documents, not WIP. Staged when this pass-along is added. **Do not commit unless asked.** Workspace copies also live under `/home/rocky/.pass-along/`.

Game files at HEAD are clean.

## Open threads

1. **Playtest the new systems** (new this session, unfinished feel). Implementation is live; feel is not. Done = a human walked a trip on https://johndamask.com/games/surf-rats/ after `66440d3` and said whether waves, retrieves, snags, and the box are too mean or too soft. Sidebar `tech-line` shows retrieve hints.
2. **Map marker alignment** (carried, never in scope this session). `SPOTS[]` ~line 474 still from a 100px grid. Done = overlay matches printed labels on `montauk-map.jpg`. Fix in `jbdamask/games`, not `jbdamask.github.io`.
3. **`/tmp/games` is ephemeral** (carried). Done = a durable checkout, or work from a fresh clone.
4. **Scratch vs source of truth** (carried). `/home/rocky/surf-rats/` duplicates published files, not git. Waiting on a human: keep or delete.
5. **Pass-along chain uncommitted** (carried). Files exist in the clone; they do not travel until someone commits `.pass-along/`.
6. **Balance besides the new systems** (deferred). Weed fouling, hike minutes (~3.2 game min per hop), bite baseline still paper numbers. Technique multipliers on top of that are unfelt.

## Recommended next — this session's view

I'd play the live page and retune **rock knock-offs** (`shoveFromRock`) and **bucktail snag chance** (`updateLureWork`, rocky + deep + `lastLift > 2.6`) before more features. Those two were guessed. Darter "every other turn" is the other likely miss if the user says the retrieve feels wrong.

Consequence of skipping: the user already bounced off a 6-second trip once. If bucktails vanish in 20 seconds or slabs still dump him, the new design reads as broken rather than unfinished.

Map markers are still the old unknown. They matter, but this session's untested code is the thing that can ruin the next playthrough *before* anyone notices a mislabeled cove.

## Stated direction

The user said the trip ending in 6 seconds was wrong and to make **two minutes = one hour**. Then: waves should shove, not always knock off (unless too far back or a pointy top); lures should fish like real retrieves (popper shake, darter tap every other turn, minnow slow-steady, bucktail low-slow with lifts); limited lures that can be lost in rocks; trophy case for catch stats. Then they invoked `/pass-along`. They have not said what comes after this build.

That overrides the 23:48 recommendation (playtest and retune markers *before* feature work). Features shipped; the playtest is still open.

## Decisions and rules

- **Source of truth is `jbdamask/games`**, path `surf-rats/`, live at https://johndamask.com/games/surf-rats/. Do not add `/games/` to `jbdamask.github.io`.
- **Clock:** `elapsedMin += dt * (60 / REAL_SECONDS_PER_GAME_HOUR)` with `dt` in seconds. Never `dt * 60` again. Current scale is 120 real seconds per game hour. Help + setup forecast must match.
- Game remains one HTML file plus `montauk-map.jpg`. Lure "hot" combo is still hashed per seed; technique is a separate multiplier. Do not add a global best lure.
- Map branding/phone number stay in the JPEG pixels.
- Trophy stats persist in browser `localStorage` (`surfrats-trophies`), not the server.
- Pass-along skill that works here: `/home/rocky/.cursor/skills/pass-along/`.

## Traps

- **`dt * 60` on the clock.** Hit in production. `dt` is seconds from `performance.now()`.
- **Git identity missing on this VM.** `git commit` fails unless `GIT_AUTHOR_NAME` / `GIT_AUTHOR_EMAIL` / `GIT_COMMITTER_*` are set for that command. Do not `git config`. Existing commits use `John Damask <jbdamask@gmail.com>`.
- **`/games/` on `jbdamask.github.io` overwrites the hub.** Custom domain `johndamask.com/games/` is `jbdamask/games`.
- **Map URL via WebFetch = HTTP 500.** `curl -L -A "Mozilla/5.0" -o montauk-map.jpg <url>`.
- **Image tools do not show the map.** Spot placement from captions will drift.
- **Same-frame SPACE after cast.** Cast used to consume `spaceTap` into retrieve. Cast now clears `spaceTap` after handling so a popper does not get a free shake / minnow a jerk on the cast frame.
- **UP while lure is in is rod action, not wade.**
- **`gather.sh` uses cwd as repo root.** Run it from `/tmp/games` (or a future durable clone). Cursor workspace is `/home/rocky`.
- **Pages cache `max-age=600`.** Hard-refresh after a push.

## Waiting on a human

- Taste of the new build: waves still too mean, snags too fast, darter rhythm opaque, box too small.
- Whether `/tmp/games` should move somewhere durable, and whether `/home/rocky/surf-rats/` stays as scratch.
- Whether to commit the `.pass-along/` chain so it survives `/tmp`.

## Housekeeping

- `/tmp/games` — clone used to publish. Safe to keep; gone if `/tmp` is cleared. HEAD should be `66440d3`.
- `/tmp/venv` — leftover from earlier map analysis. Safe to delete.
- `/tmp/surfrats-check.js` — may exist from `node --check` on extracted script. Safe to delete.
- `/home/rocky/surf-rats/` — duplicate of published `index.html` + jpeg. Not git.
- `/home/rocky/.pass-along/` — workspace copy of the chain. Canonical copy for travel is `jbdamask/games/.pass-along/` once committed.
- No leftover HTTP server.
