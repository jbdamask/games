---
DATE: 2026-08-20T03:14:10+0000
TITLE: Surf Rats — Bill, storms, charity, casts, fairness pass all live at 19fae32
STATUS: in_progress
MODEL: unknown
MODEL_EFFORT: unknown
HARNESS: cursor
SESSION_ID: 62b65327-2abe-43c9-a8f3-cff7b409af6e
REPO: jbdamask/games
WORKING_DIR: /home/rocky
BRANCH: main
HEAD_SHA: 19fae32
UPSTREAM: origin/main (ahead 0)
WORKTREE: clean
ISSUE_TRACKER: GitHub
OPEN_LOOPS:
  - "No human playtest of 19fae32 fairness pass — fight burst rhythm, snag rates, storm/lightning feel untested on live site"
  - "Map marker coordinates still estimated vs Wetzel labels on montauk-map.jpg"
  - "Clone only at /tmp/games — ephemeral"
  - "/home/rocky/surf-rats/ scratch copy may drift from git"
  - "This pass-along file staged but not committed unless user asks"
RECOMMENDED_NEXT: Play one full trip on https://johndamask.com/games/surf-rats/ after hard-refresh and confirm bass land with SPACE bursts, snags feel annoying-but-fair, and storms read as risk/reward — user already flagged snags and uncatchable fish twice this session.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-20-0045-PASS-ALONG.md
PRIME: ["open https://johndamask.com/games/surf-rats/", "git -C /tmp/games status || gh repo clone jbdamask/games /tmp/games"]
---

## Start here

**Surf Rats** is live at https://johndamask.com/games/surf-rats/ on `jbdamask/games` **`main` @ `19fae32`**. Pages last checked **03:14 UTC**: status `built`, commit `19fae32`. One HTML game plus assets in `surf-rats/`. Predecessor pass-along: `.pass-along/2026-08-20-0045-PASS-ALONG.md` (stale at `66440d3`).

First commands: `git -C /tmp/games log -1 --oneline` · hard-refresh the live URL · read `surf-rats/index.html` if tuning feel.

## May have moved since I stopped

- **Pages:** built at `19fae32` as of 03:14 UTC. Recheck `gh api repos/jbdamask/games/pages/builds/latest`. CDN `max-age=600` — hard-refresh if the page looks old.
- **`/tmp/games`:** should be clean, tracking `origin/main`. If missing, `gh repo clone jbdamask/games /tmp/games`.
- **No CI, PRs, or background jobs** besides GitHub Pages.

## What this session did

Cursor session `62b65327` (continued from recap of `eb4d41bc` / `34a867b9` work). User iterated on live game after Montauk Bill shipped.

- **Random callsigns** (`ac1268b`): removed name input; assign Googin / Herb / Dangler / Spotburn at launch. Fixed misleading “draw/pick” copy (`19fae32`).
- **Storms & lightning** (`ac1268b`): per-spot rain (drizzle +22% bite, downpour +55%), escalating screen flashes and strike risk while on rocks.
- **Kids Need More charity** (`ac1268b`): links on title screen, help modal, Bill talk dialog → https://kidsneedmore.org/
- **Lure cast distances + camera** (`f1392a3`): bucktail shortest → popper farthest; surf view pans left on outer rocks; idle hang-up if lure left out unreeling.
- **Snag & fight fairness** (`19fae32`): user said snags too easy and fish impossible to catch. Softened rock snags (longer pop-free window, lower rates). Rebalanced fight math — holding SPACE snapped line before land bar filled; now pump in bursts, faster tension recovery, picky-spot bite floor, seal/shark grace period.
- **Trip-start UX** (`19fae32`): toast `YOU ARE <CALLSIGN>`, log `callsign assigned: …`, setup says callsign is assigned not chosen.

All game changes in **`surf-rats/index.html`** only (plus existing PNG assets).

## Commits

Since predecessor `66440d3`, all **pushed** to `origin/main`:

- `7707148` Fix UP rod input; lose fish when waves wash off rocks; commit pass-along chain.
- `540c3fa` Custom logo on title screen.
- `8fc82e5` Restore SURF RATS title text on splash.
- `c5390c7` Montauk Bill rival, talk UI, score race, bill PNGs.
- `c60f58d` Restore `currentSpot()` — LAUNCH TRIP crash fix.
- `ac1268b` Storm fishing, beach callsigns, Bill's charity link.
- `f1392a3` Lure cast distances, idle snags, far-rock camera pan.
- `19fae32` Fairer fish fights, softer rock snags, callsign copy fix.

## Uncommitted work

**This pass-along file** — will be staged when written; not committed unless user asks. Game tree clean at HEAD.

## Open threads

1. **Live feel untested after `19fae32`** (in scope this session, user feedback addressed in code only). Done = user plays a trip and says snags/fights/storms feel fair.
2. **Map markers** (carried, never in scope). `SPOTS[]` ~line 594 still estimated. Fix in `jbdamask/games`, not `jbdamask.github.io`.
3. **`/tmp/games` ephemeral** (carried).
4. **`/home/rocky/surf-rats/` scratch copy** (carried). Synced manually after edits; not git.
5. **Balance still paper-tuned** (deferred). Weed, hike time, Bill AI cadence, storm/lightning rates adjusted by formula not playtest.

## Recommended next — this session's view

I'd play the live page once on `19fae32` focusing on **landing a bass** (SPACE in bursts, not held) and **one rocky spot with a bucktail** — the user complained twice about snags and uncatchable fish; we tuned twice without their confirmation. If it still feels wrong, adjust `updateLureWork` snag block and the `G.fish` fight block (~lines 1510–1785 in `surf-rats/index.html`) rather than adding features.

Consequence of skipping: the fairness pass is the latest deploy but unvalidated; another feature layer on top of wrong feel wastes time.

Map markers remain the old deferred item — important but not what blocked the last two user messages.

## Stated direction

User did not name a next feature after the fairness push. They said **yes** to deploy `19fae32`, then invoked **`/pass-along`**. Prior requests in this arc: no name entry (random callsigns), storms/lightning, charity link, cast distances, camera pan, fewer snags, catchable fish.

## Decisions and rules

- **Source of truth:** `jbdamask/games` → `surf-rats/`, live at https://johndamask.com/games/surf-rats/. Do **not** add `/games/` to `jbdamask.github.io`.
- **Callsigns:** assigned at `startTrip()` via `pickPlayerName()` — never prompt. Copy must say **assigned**, not pick/draw/choose.
- **Fight:** land fish with **SPACE bursts**; continuous pump builds tension faster than land progress (by design after `19fae32`).
- **Charity:** Kids Need More → https://kidsneedmore.org/ on title, help, Bill talk.
- **Clock:** `REAL_SECONDS_PER_GAME_HOUR = 120`. Never `elapsedMin += dt * 60`.
- **Git identity on VM:** set `GIT_AUTHOR_*` / `GIT_COMMITTER_*` per commit; do not `git config`.
- **Pass-alongs:** commit to repo when user asks; chain lives in `.pass-along/`.

## Traps

- **`dt * 60` on clock** — production bug once; still the #1 regression.
- **Fight felt “impossible”** because old math was `tension += 22 + inch*0.35` per second while pumping — holding SPACE always snapped. Hint now says burst pump.
- **“Draw callsign” copy** — user read as pick a name; use **assigned**.
- **Snag tuning whack-a-mole** — idle snag, bucktail crank snag, and fast-reel snag are three separate blocks in `updateLureWork`.
- **Pages cache `max-age=600`.** Hard-refresh after push.
- **`gather.sh` from wrong cwd** — run from `/tmp/games`.
- **`/tmp/games` gone after reboot** — re-clone.

## Waiting on a human

- Taste call on `19fae32`: snags, fight rhythm, storm/lightning, callsign toast.
- Whether to commit this pass-along to `main`.
- Durable clone location vs keeping `/tmp/games`.

## Housekeeping

- `/tmp/games` — git clone, HEAD `19fae32`, clean. Primary dev checkout.
- `/home/rocky/surf-rats/` — scratch mirror of published files; not git.
- `/home/rocky/.pass-along/` — workspace copy of pass-along chain; sync when repo copy updates.
- `/tmp/surfrats-check.js` — optional syntax-check extract; safe to delete.
- No leftover HTTP server.
