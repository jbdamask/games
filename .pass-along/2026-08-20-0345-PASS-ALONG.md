---
DATE: 2026-08-20T03:45:07+0000
TITLE: Surf Rats — directed casts, school finding, map tooltip pass live at be31db2
STATUS: in_progress
MODEL: unknown
MODEL_EFFORT: unknown
HARNESS: cursor
SESSION_ID: 62b65327-2abe-43c9-a8f3-cff7b409af6e
REPO: jbdamask/games
WORKING_DIR: /tmp/games
BRANCH: main
HEAD_SHA: be31db2
UPSTREAM: origin/main (ahead 0)
WORKTREE: clean
ISSUE_TRACKER: GitHub
OPEN_LOOPS:
  - "Map tooltips still not perfect after be31db2 — user said better, not perfect; east/south shore labels vs Wetzel leader lines still need eyeball"
  - "No human playtest of school-finding bite rates, charge-cast feel, or fish-on-the-line graphics on live site"
  - "Fight burst rhythm, snags, storms from 19fae32 still unconfirmed by a full trip"
  - "Clone only at /tmp/games — ephemeral"
  - "/home/rocky/surf-rats/ scratch copy may drift from git"
  - "This pass-along file staged but not committed unless user asks"
RECOMMENDED_NEXT: Hover the live map at North Bar through Caswell's/Tea House and nudge remaining SPOTS[] pixels that still miss Wetzel leader lines — user just said alignment is better not perfect, so that is the live complaint.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-20-0314-PASS-ALONG.md
PRIME: ["open https://johndamask.com/games/surf-rats/", "git -C /tmp/games status || gh repo clone jbdamask/games /tmp/games"]
---

## Start here

**Surf Rats** is live at https://johndamask.com/games/surf-rats/ on `jbdamask/games` **`main` @ `be31db2`**. Pages last checked **03:45 UTC**: status `built`, commit `be31db2`. All game logic still lives in `surf-rats/index.html` plus PNG/JPG/favicon assets. Predecessor: `.pass-along/2026-08-20-0314-PASS-ALONG.md` (stale at `19fae32`).

First commands: `git -C /tmp/games log -1 --oneline` · hard-refresh the live URL · if tuning map, edit `SPOTS[]` in `surf-rats/index.html` (~line 645) against `montauk-map.jpg`.

## May have moved since I stopped

- **Pages:** built at `be31db2` as of 03:45 UTC. Recheck `gh api repos/jbdamask/games/pages/builds/latest`. CDN `max-age=600` — hard-refresh if the page looks old.
- **`/tmp/games`:** should be clean, tracking `origin/main`. If missing, `gh repo clone jbdamask/games /tmp/games`.
- **No CI, PRs, or background jobs** besides GitHub Pages.

## What this session did

Cursor session `62b65327` continued after the 03:14 pass-along. User shipped features, then play-feel and map UX.

- **Directed charge casts** (`b3e0df6`): hold SPACE to aim (◀▶ lateral, ▲▼ depth) and charge a Wii-golf power meter; release to throw. Lure max reach still differs by type.
- **Fish-on graphics + log** (`b3e0df6`): bass drawn on the lure at line end (`G.lureX`/`G.lureY`); prominent `>>> FISH ON! <<<` log with estimated size (SCHOOLIE–COW).
- **Welcome copy** (`7791731`): setup/toast/log say `Welcome <NAME>. Can you outfish Montauk Bill?` — no callsign-assignment disclaimer.
- **Harder fishing / schools** (`f58088c`): blind bite rate cut; probe meter while working lure; `MARKED FISH` / `BASS ROLLED IN CLOSE` opens a short school window. Empty retrieve burns remaining school casts.
- **Help trimmed** (`bf3bbf2`): no per-lure how-to in HOW TO FISH. Bill talk still has the real techniques. Bite/find rates scale with `G.techMul` (squared on bites).
- **Favicon** (`98212fd`): `favicon.ico`, `favicon-32.png`, `apple-touch-icon.png` from the game logo.
- **Map tooltips** (`0a64f63` then `be31db2`): first pass used nearest-spot pick + canvas tooltip (user still saw south-shore drift). Second pass: DOM overlay `#map-tooltip` anchored to marker pixels, canvas mouse scaled by `clientWidth` vs CSS rect, and **30 spots recalibrated** North Bar → Town Beaches against `montauk-map.jpg`. User: **better, not perfect**.

## Commits

Since predecessor `19fae32`, all **pushed** to `origin/main`:

- `b3e0df6` Add directed charge casts and clearer fish-on feedback.
- `7791731` Replace callsign disclaimer with a welcome line.
- `f58088c` Make bass harder to find; reward working water and schools.
- `bf3bbf2` Trim lure tips from help; lean on Bill and presentation.
- `0a64f63` Fix map tooltips picking the wrong spot in dense clusters.
- `98212fd` Add favicon derived from the Surf Rats game logo.
- `be31db2` Fix map tooltips and recalibrate spot coordinates.

Also on that tree (already in predecessor): `.pass-along/2026-08-20-0314-PASS-ALONG.md` landed inside `b3e0df6` because it was staged when the first commit of this session ran.

## Uncommitted work

**This pass-along file** — staged when written; not committed unless user asks. Game tree clean at HEAD.

## Open threads

1. **Map labels vs Wetzel lines** (in scope this session, improved not finished). User confirmed hover pixels match map lines; tooltip names were wrong because `SPOTS[]` drifted south/east. Recalibration in `be31db2` is eyeball-tuned. Done = user says Caswell's/Tea House/North Bar match the printed labels.
2. **School / bite feel untested live** (in scope). Formula-tuned. Done = a trip where most casts are empty until marked fish, then a short bang-them window.
3. **Charge-cast + fish-on-line untested** (in scope). Done = player can aim and max-charge, and hooked bass stays on the lure.
4. **Fairness pass from `19fae32`** (carried). Snags/fights/storms still never human-confirmed after that deploy.
5. **`/tmp/games` ephemeral** (carried).
6. **`/home/rocky/surf-rats/` scratch** (carried). Synced after most edits; not git.
7. **Balance still paper-tuned** (deferred). Weed, hike time, Bill AI cadence, storm rates, school probe speed.

## Recommended next — this session's view

I'd hover the live map at **North Bar → Montauk Point → Caswell's / Tea House / Coconuts** after hard-refresh and nudge any remaining `SPOTS[]` x/y that still sit off the leader-line endpoints. User just said alignment is better not perfect; that is the only unfinished complaint from this session.

Consequence of skipping: next feature work on top of a map the user already flagged twice.

School bite rates and charge-cast feel are also unplaytested — important, but not what they last talked about.

## Stated direction

After map work: **"better. not perfect but better."** then **`/pass-along`**. No next feature named. Prior requests this session: directed casts + Wii power bar, fish-on in chat with size + fish on the lure, welcome copy not callsign disclaimer, fewer easy hookups / work to find fish, basic lure help only (learn from Bill), push/deploy, favicon, map tooltip alignment.

## Decisions and rules

- **Source of truth:** `jbdamask/games` → `surf-rats/`, live at https://johndamask.com/games/surf-rats/. Do **not** add `/games/` to `jbdamask.github.io`.
- **Callsigns:** assigned at setup (`openSetup` → `pickPlayerName()`), kept through `startTrip()`. Copy: **Welcome NAME. Can you outfish Montauk Bill?** Never explain random assignment on the setup screen.
- **Cast:** hold SPACE to wind up; arrows aim; release throws. Climb boulder still uses SPACE tap when near a rock, not a charge.
- **Fight:** bass is on `G.lureX`/`G.lureY`. Pump SPACE in bursts.
- **Finding fish:** `G.school` probe while lure is working; `schoolBiteMul()` is tiny until `active`. Presentation `techMul` squared into bite chance. Help text must not spoil lure technique — Bill does.
- **Charity:** Kids Need More → https://kidsneedmore.org/
- **Clock:** `REAL_SECONDS_PER_GAME_HOUR = 120`. Never `elapsedMin += dt * 60`.
- **Git identity on VM:** set `GIT_AUTHOR_*` / `GIT_COMMITTER_*` per commit; do not `git config`.
- **Pass-alongs:** commit to repo when user asks; chain lives in `.pass-along/`.

## Traps

- **Map hover vs tooltip names were two bugs.** Hover hit-testing used last-in-range (fixed `0a64f63`). Remaining mismatch was **wrong `SPOTS[]` coords**, not CSS. Do not "fix" tooltips again without overlaying dots on `montauk-map.jpg`.
- **Canvas tooltip + guessed `label.length * 8` width** misaligned in dense clusters. DOM `#map-tooltip` is the current approach.
- **`canvasPos` must scale** `(clientX - rect.left) * (viewCss.w / rect.width)` — raw CSS pixels vs logical canvas size diverge with DPR.
- **`dt * 60` on clock** — still the #1 regression.
- **Fight felt impossible** if SPACE is held — burst pump by design after `19fae32`.
- **Snag tuning** — idle, bucktail crank, and fast-reel are three blocks in `updateLureWork`.
- **Pages cache `max-age=600`.** Hard-refresh after push. Favicon especially sticky.
- **`gather.sh` from wrong cwd** — run from `/tmp/games`.
- **`/tmp/games` gone after reboot** — re-clone.
- **First commit this session accidentally included the previous staged pass-along** (`2026-08-20-0314`) inside `b3e0df6`. Harmless; don't restage old pass-alongs by accident.

## Waiting on a human

- Taste call: map labels around the Point and south shore after `be31db2`.
- Taste call: school hunting vs "every cast" — they asked for work to find fish; unconfirmed live.
- Whether to commit this pass-along to `main`.
- Durable clone location vs keeping `/tmp/games`.

## Housekeeping

- `/tmp/games` — git clone, HEAD `be31db2`, clean. Primary checkout.
- `/home/rocky/surf-rats/` — scratch mirror; not git.
- `/home/rocky/.pass-along/` — workspace copy of the chain; sync when repo copy updates.
- `/tmp/map-debug.jpg`, `/tmp/map-num.jpg`, `/tmp/map-cal.jpg` — calibration overlays; safe to delete.
- No leftover HTTP server.
