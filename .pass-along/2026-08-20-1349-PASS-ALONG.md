---
DATE: 2026-08-20T13:49:44+0000
TITLE: Surf Rats UFO visitors — alien dialogue, drop fishing fix live
STATUS: in_progress
MODEL: unknown
MODEL_EFFORT: unknown
HARNESS: cursor
SESSION_ID: 62b65327-2abe-43c9-a8f3-cff7b409af6e
REPO: jbdamask/games
WORKING_DIR: /tmp/games
BRANCH: main
HEAD_SHA: f5af8a5
UPSTREAM: origin/main (0 ahead, 0 behind)
WORKTREE: clean
ISSUE_TRACKER: GitHub
OPEN_LOOPS:
  - "UFO_CHANCE still 0.5 in index.html — raised for browser testing, not production 1%"
  - "No human playtest on live site of alien school bites, 50″ fish rate, or full trip with visitors"
  - "Map tooltips / SPOTS[] — predecessor said better not perfect; not revisited this session"
  - "Fight burst rhythm, snags, storms from 19fae32 still unconfirmed by a full trip"
  - "School-finding bite rates and charge-cast feel from earlier commits still unplaytested live"
  - "/tmp/games clone is ephemeral — re-clone after reboot"
  - "/home/rocky/surf-rats/ scratch copy may drift from git"
  - "Pass-along chain in repo stops at 5284bf3 inside games; home ~/.pass-along/ has copies but games .pass-along/ only had files through 0345 until this write"
RECOMMENDED_NEXT: Hard-refresh https://johndamask.com/games/surf-rats/ and play one full trip — confirm hooks after alien beam (hot school + 50″ size bias). If good, drop UFO_CHANCE to 0.01 and commit this pass-along.
SCOPE_COMPLETE: false
PREDECESSOR: .pass-along/2026-08-20-0345-PASS-ALONG.md
PRIME: ["gh api repos/jbdamask/games/pages/builds/latest --jq '{status,commit: .commit[0:7]}'", "git -C /tmp/games log -1 --oneline", "open https://johndamask.com/games/surf-rats/"]
---

## Start here

**Surf Rats** live at https://johndamask.com/games/surf-rats/ on `jbdamask/games` **`main` @ `f5af8a5`**. Predecessor: `.pass-along/2026-08-20-0345-PASS-ALONG.md` (stale at `be31db2` — before the entire UFO/alien arc).

First commands: confirm Pages built at `f5af8a5` · `git -C /tmp/games log be31db2..HEAD --oneline` · hard-refresh the game URL.

## May have moved since I stopped

- **Pages:** verify `gh api repos/jbdamask/games/pages/builds/latest` — expect **built** at `f5af8a5` (deployed after fishing fix push ~13:37 UTC).
- **`UFO_CHANCE`:** still **0.5** in repo — every other spot arrival has a coin-flip UFO cutscene; revert before calling the feature shippable for real players.
- **Alien asset:** `surf-rats/alien-montauk.png` (~1.3MB) must exist on Pages; 503 on any asset during deploy was reported earlier — recheck if map draw throws `drawImage` broken state (guard may still be missing on `mapImg`).

## What this session did

- **UFO easter egg** (`3ac5f48` … `f5af8a5`): 1-in-100 visitors (later 50% for testing) — saucer cutscene, typewriter **VISITORS** overlay (`#alien-talk` + `alien-montauk.png`), peace line with m'kay sweetener, **no auto-score**; aliens mark drop spot and player fishes for 50 lb bass.
- **Fishing felt dead after alien visit** — root cause: only `fishSize()` biased to 50″ on hook; **`biteChance` / school never activated**. Fixed in `f5af8a5` with `activateAlienDrop()` (hot school, 10 casts, probe maxed) plus alien-spot bite multiplier when school faded.
- **Trip clock** (`4d766f4`): entire `update()` loop pauses while `G.ufo` is set so trip time does not drain during cutscene (regression from only freezing during `say` phase).
- **Alien portrait** (`8345d9f`): user-supplied `alien-montauk.png` in dialogue box replaces canvas UFO sprite.

## Commits

Since predecessor `be31db2` (all pushed to `origin/main`):

- `f5af8a5` — Activate hot school when aliens drop bass so bites actually happen.
- `8345d9f` — alien-montauk art; 50% UFO for testing.
- `4d766f4` — trip clock pause during UFO; restore 1% spawn (superseded by 8345d9f for test rate).
- `67b668c` — typewriter alien dialogue overlay.
- `6c8a8b1` — m'kay sweetener copy.
- `caf9ef8` — peace message + catchable fish (removed auto-grant beam).
- `5cc2245` / `3ac5f48` — 50% then 1% UFO testing commits.
- `5284bf3` — prior pass-along committed into repo (accidental chain tip).

## Uncommitted work

None — tree clean at `f5af8a5`. **This pass-along file** is new and unstaged until someone `git add`s it.

## Open threads

1. **UFO test rate (in scope, unfinished for production)** — `UFO_CHANCE = 0.5` in `surf-rats/index.html`. Done for production = `0.01`. User was actively testing in browser.
2. **Alien drop fishing (in scope this session, fix landed)** — verify live after `f5af8a5`; done = hook bass at alien-marked spot within a few casts post-beam.
3. **Map tooltips / SPOTS[] (carried from 0345)** — user said "better, not perfect"; not touched this session.
4. **Live playtest matrix (carried)** — school-finding, charge-cast, fish-on-line graphics, fairness pass from `19fae32`; none confirmed on https://johndamask.com/games/surf-rats/ this session.
5. **`/tmp/games` ephemeral (carried)** — clone path; disappears on reboot.
6. **Scratch `~/surf-rats/` drift (carried)** — sync from `/tmp/games/surf-rats/` after pulls.

## Recommended next — this session's view

I'd play one trip on the live URL with hard-refresh, trigger an alien visit, and confirm bites during the hot school — that's the acceptance test for `f5af8a5`. If it passes, set `UFO_CHANCE` back to **0.01**, commit this pass-along, and only then treat the visitor feature as production-ready. Skipping the test rate revert leaves half of spot arrivals in a QA-only cutscene.

## Stated direction

User asked to **commit/push/deploy** the UFO feature, then iterate on alien copy and on-screen dialogue. Reported **fishing still broken** after aliens ("haven't caught a fish since we introduced this feature") whether or not a disc landed — led to school/bite fix, not copy changes. Confirmed alien UI **works** after `alien-montauk.png`; map `drawImage` 503 was transient.

## Decisions and rules

- **Do not** auto-grant ten fish — aliens **drop** bass in the water; player hooks them (`activateAlienDrop` + `fishSize` 18% for 50″).
- **Pause whole game loop** during `G.ufo` (same weight as Bill talk), not just `updateSurf`.
- **Alien overlay** is `#alien-talk` typewriter; Bill's `#talk` is separate.
- **`tryUfoEncounter`** on travel complete and first **FISH HERE** at a spot; blocks fishing until `G.ufo` clears.
- **`G.alienSpot === G.spotIndex`** prevents repeat UFO at same drop spot; bite boost applies when `alienSpot` matches current spot.
- **Git identity on VM:** `GIT_AUTHOR_*` / `GIT_COMMITTER_*` per commit — never `git config`.
- **Pass-alongs:** commit when user asks; chain in `.pass-along/`.

## Traps

- **Aliens without bites** — if only `fishSize()` is wired, players see flavor and zero hookups; always pair alien drop with **`activateAlienDrop()`** (or equivalent school activation).
- **Trip clock during UFO** — if only `say` phase freezes time, player loses trip minutes during `in`/`beam`/`out`; freeze entire `update()` while `G.ufo`.
- **50% UFO** — feels like the game is broken; it's a test knob left on.
- **`resetBill()` inside `tryUfoEncounter`** when switching to surf — wipes lure/school; intentional on encounter start, confusing if you debug mid-cast.
- **`canCastWindup` during `G.ufo`** — now returns false; casting during cutscene used to leave `castWindup` stuck.
- **Canvas `drawImage(mapImg)`** — throws if map asset 503'd; check network before blaming game logic.
- **`gather.sh` harness** — reports `unknown` for Cursor; set `HARNESS: cursor` and session id from predecessor unless transcript path found.

## Waiting on a human

- Confirm live fishing feels fair after alien visit (or say it still doesn't).
- Whether to **commit this pass-along** to `main`.
- Whether to **drop UFO to 1%** before sharing the game wider.

## Housekeeping

- **Primary checkout:** `/tmp/games` (git clone of `jbdamask/games`), HEAD `f5af8a5`, clean.
- **Scratch mirror:** `/home/rocky/surf-rats/` — not git; copy from clone when needed.
- **Pass-along copies:** `~/.pass-along/` mirrors chain; repo `.pass-along/` had through `0345` until this file — add and commit when user wants.
- **Assets on disk in clone:** `surf-rats/alien-montauk.png`, `bill-*.png`, favicons, `index.html` monolith.
- No background servers left running.
