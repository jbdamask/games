---
DATE: 2026-08-20T14:47:29+0000
TITLE: Surf Rats UFO shipped — 5% spawn, playtest passed, deployed
STATUS: milestone_complete
MODEL: composer-2.5-fast
MODEL_EFFORT: unknown
HARNESS: cursor 2026.08.11-e8db854
SESSION_ID: 9221d9eb-d7f3-4514-91b7-a5da872005b4
REPO: jbdamask/games
WORKING_DIR: /tmp/games
BRANCH: main
HEAD_SHA: 0aa036c
UPSTREAM: origin/main (0 ahead, 0 behind)
WORKTREE: dirty
ISSUE_TRACKER: GitHub
OPEN_LOOPS:
  - "Map tooltips / SPOTS[] — user said better not perfect; never revisited"
  - "Broader live playtest pass still open — school-finding, charge-cast, fight burst rhythm, snags, storms (19fae32 era)"
  - "UFO_CHANCE is 5% (0.05) — user chose 5%, not predecessor's 1% target"
  - "/tmp/games clone is ephemeral — re-clone after VM reboot"
  - "/home/rocky/surf-rats/ scratch copy may drift from git"
  - "~/.cursor/skills/pass-along updated from john-claude-skills main (Cursor harness in gather.sh) — VM-only, not in any repo"
RECOMMENDED_NEXT: Nothing urgent on Surf Rats UFO — user called the arc shipped. If picking up game polish, map tooltips / SPOTS[] or a full-trip live playtest pass are the natural next items.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-20-1349-PASS-ALONG.md
PRIME: ["gh api repos/jbdamask/games/pages/builds/latest --jq '{status,commit: .commit[0:7]}'", "git -C /tmp/games log -1 --oneline", "open https://johndamask.com/games/surf-rats/"]
---

## Start here

**Surf Rats** UFO visitor feature is **shipped** on `jbdamask/games` **`main` @ `0aa036c`**, live at https://johndamask.com/games/surf-rats/. Predecessor: `.pass-along/2026-08-20-1349-PASS-ALONG.md` (UFO arc in progress at `f5af8a5`).

First commands: confirm Pages at `0aa036c` · `git -C /tmp/games log f5af8a5..HEAD --oneline` · hard-refresh the game URL.

## May have moved since I stopped

- **Pages:** verified **built** at `0aa036c` (~13:58 UTC). Live HTML shows `UFO_CHANCE = 0.05`.
- **This pass-along file:** new and unstaged until someone `git add`s it.
- **`~/.cursor/skills/pass-along`:** updated from `jbdamask/john-claude-skills` main during this session (adds Cursor CLI to `gather.sh`). Lives on the VM only — not committed anywhere.

## What this session did

- **Session recap** — reconstructed state from `1349` pass-along; verified Pages at `f5af8a5`, fishing fix live, UFO still at 50% test rate.
- **UFO spawn rate (`0aa036c`)** — user playtested post-`f5af8a5` fix and confirmed fishing works again; set `UFO_CHANCE` to **0.05** (5%, not predecessor's 1% recommendation).
- **Commit + push + deploy** — `0aa036c` pushed to `origin/main`; Pages built and live.
- **Pass-along chain** — committed predecessor `1349` pass-along into repo as part of `0aa036c`.
- **Skill housekeeping** — synced `~/.cursor/skills/pass-along` from GitHub (Cursor harness detection in `gather.sh`).

## Commits

Since predecessor `f5af8a5` (all pushed):

- `0aa036c` — Set UFO spawn rate to 5% after live playtest; add session pass-along (`1349`).

## Uncommitted work

- **This file** — `.pass-along/2026-08-20-1447-PASS-ALONG.md` (new, not yet staged).
- **`~/.cursor/skills/pass-along/`** — updated SKILL.md + gather.sh on VM; outside the games repo.

## Open threads

1. **Map tooltips / `SPOTS[]` (carried, deferred)** — user said "better, not perfect"; not touched this or prior UFO session. Ready when someone wants polish.
2. **Broader live playtest matrix (carried, deferred)** — school-finding bite rates, charge-cast feel, fight burst rhythm, snags, storms from `19fae32` era; none confirmed on live URL this session. UFO-specific playtest passed; general fairness pass still open.
3. **UFO rate at 5% not 1% (user decision, not a bug)** — predecessor recommended 1%; user explicitly chose 5% after playtest. Done = leave at 0.05 unless user asks to change.
4. **`/tmp/games` ephemeral (carried)** — clone path; gone after reboot. Re-clone `jbdamask/games`.
5. **Scratch `~/surf-rats/` drift (carried)** — non-git mirror; sync from `/tmp/games/surf-rats/` after pulls if editing locally.
6. **Pass-along skill update (VM-only)** — `~/.cursor/skills/pass-along` now has Cursor harness support; not versioned in any repo on this machine.

## Recommended next — this session's view

I'd treat the UFO visitor arc as **done** — user asked "we done?" and accepted 5% as the production rate after confirming fishing post-alien-visit works. No urgent Surf Rats work unless the user wants polish: map tooltips/`SPOTS[]` or a full-trip live playtest of the older mechanics (school-finding, charge-cast, fight rhythm) would be the natural follow-ups, not more UFO wiring.

Skipping those is fine; the game is playable and the visitor feature is deployed.

## Stated direction

User said **fishing seems to work again** — reduce UFO chance to **5%** and commit the staged pass-along. Then **commit/push/deploy**. Later asked **"we done?"** — accepted the arc as shipped with optional backlog called out. Also asked to **update the pass-along skill** from `jbdamask/john-claude-skills` and whether a session restart was needed (answered: new chat for catalog refresh; scripts read from disk immediately).

## Decisions and rules

- **UFO production rate is 5% (`0.05`)** — user's call after playtest, overriding predecessor's 1% recommendation.
- **Alien drop fishing** — `activateAlienDrop()` + `fishSize()` bias; confirmed working by user this session.
- **Do not auto-grant fish** — aliens drop bass in the water; player hooks them.
- **Trip clock pauses for full `G.ufo` sequence** — not just dialogue phase.
- **Git identity on VM:** `GIT_AUTHOR_*` / `GIT_COMMITTER_*` per commit — never `git config`.
- **Pass-alongs:** commit when user asks; chain in `.pass-along/`.

## Traps

- **50% UFO felt broken** — was a test knob; now 5% live. Hard-refresh if browser cached old `index.html`.
- **Aliens without bites** — if only `fishSize()` is wired, zero hookups; always pair with `activateAlienDrop()`.
- **`gather.sh` from `/tmp/games`** — Cursor session is keyed to `/home/rocky`, so harness block may show `unknown` when run from the games clone; run from home or set session id manually if frontmatter matters.
- **`/tmp/games` gone after reboot** — re-clone before any git work.
- **Pass-along attached skill may be stale** — Cursor snapshots skill catalog at chat start; scripts on disk are always current.

## Waiting on a human

- Whether to **commit this pass-along** to `main`.
- Whether **5% UFO** stays or should drop to 1% before wider sharing.
- Whether to tackle **map tooltips** or **broader playtest pass** next.

## Housekeeping

- **Primary checkout:** `/tmp/games`, HEAD `0aa036c`, clean except this new pass-along file.
- **Scratch mirror:** `/home/rocky/surf-rats/` — sync from clone if needed.
- **Pass-along copies:** write to repo `.pass-along/`; mirror to `~/.pass-along/` when useful.
- **Live game:** https://johndamask.com/games/surf-rats/ — Pages built at `0aa036c`.
- No background servers left running.
