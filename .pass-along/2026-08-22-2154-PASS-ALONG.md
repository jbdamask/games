---
DATE: 2026-08-22T21:54:34+0000
TITLE: Surf Rats balance pass, Bill tips, social cards, hook-shake tuning
STATUS: in_progress
MODEL: unknown
MODEL_EFFORT: unknown
HARNESS: cursor
SESSION_ID: unknown
REPO: jbdamask/games
WORKING_DIR: /home/rocky/games
BRANCH: main
HEAD_SHA: 776c125
UPSTREAM: origin/main (0 ahead, 0 behind)
WORKTREE: clean
ISSUE_TRACKER: GitHub
ISSUES: []
TASK_TRACKER: none
OPEN_LOOPS:
  - "Hook-shake / idle-reel feel needs live playtest after 776c125 release-on-spacebar fix"
  - "UFO_CHANCE is 5% (0.05) — user thought it was ~10%; 89ec377 once regressed it to 50%"
  - "/home/rocky/surf-rats/ is a scratch copy with no git — can drift from games/surf-rats/"
  - "Stray jbdamask/surf-rats repo was created by mistake early session; user deleted it"
  - "Map tooltips / SPOTS[] polish still open from prior pass-along"
  - "Broader live playtest pass still open (school-finding, charge-cast, snags, storms)"
RECOMMENDED_NEXT: Live-playtest hook-shake on http://johndamask.com/games/surf-rats/ — burst pump SPACE and confirm shakes happen only after ~3s idle, not during normal reeling; tweak FISH_IDLE_GRACE / FISH_IDLE_JUMP_CHANCE in surf-rats/index.html if needed.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-20-1447-PASS-ALONG.md
PRIME: ["cd /home/rocky/games && git pull origin main", "gh api repos/jbdamask/games/pages/builds/latest --jq '{status,commit: .commit[0:7]}'", "open https://johndamask.com/games/surf-rats/"]
---

## Start here

Surf Rats on `main` at `776c125` — a large balance/UI pass landed today and is pushed; Pages should be built. Canonical repo is **`jbdamask/games`**, game lives in **`surf-rats/`**. Read predecessor `.pass-along/2026-08-20-1447-PASS-ALONG.md` for UFO arc context.

```bash
cd /home/rocky/games && git pull origin main
gh api repos/jbdamask/games/pages/builds/latest --jq '{status,commit: .commit[0:7]}'
```

## May have moved since I stopped

- GitHub Pages build for `776c125` was not re-verified after the final commit; confirm `status: built` and spot-check live hook-shake behavior.
- User may still be playtesting on live URL — uncommitted local edits would only exist in `/home/rocky/surf-rats/` scratch copy, not in git.

## What this session did

- **Storm/alien odds** (`89ec377`, later fixes): rain 25% at spot, lightning 10% per flash, post-alien 50 lb bass hook 40%. **Trap:** that commit also accidentally set `UFO_CHANCE` back to `0.5` — fixed in `2c914fb`.
- **Montauk Bill tips** (`0fbce6a`, `99b01fb`, `c384046`): randomized full spot/lure/color advice; 60% bite boost for 60s when matched; optional charity and catch-and-release append lines (20% each, independent rolls).
- **Social cards** (`dc1d863`, `7c9f18e`, `2cdea21`): OG/Twitter meta; user-added `surf-rats-*-card.jpg` images wired per platform.
- **Trophy case** (`eef9287`, `0e64ba2`): `striped-bass.png` on mounts instead of green silhouette bar.
- **Fish fight idle penalties** (`8082fb7`, `b64a9e1`, `776c125`): hook-shake and seal/shark steal when not pumping; iterated twice after user feedback. Latest logic: timer resets on **spacebar release**, counts only while spacebar up, 3s grace then 12%/sec shake, 20s → predator.
- **Curse bubble** (`d6118b5`): `$#&@!!!` above player for 3s on fish loss.
- **Player vs Montauk Bill naming** (`417eb21`): `G.bill` → `G.player` for surf-view angler; UI text distinguishes player name from Montauk Bill rival.
- **Loss reporting** (`776c125`): toast + REPORT log + results/trophy list explicit loss reasons.
- **Repo hygiene:** established `/home/rocky/games` as canonical clone; removed stale git from `/home/rocky/surf-rats/` scratch workspace.

## Commits

Since predecessor `0aa036c` (17 commits, all pushed to `origin/main`):

| SHA | Summary |
|-----|---------|
| `776c125` | Hook-shake on release + loss reasons in report/results |
| `b64a9e1` | Hook-shake grace for burst reeling (6%/sec) |
| `2c914fb` | Restore UFO spawn to 5% |
| `417eb21` | G.player vs Montauk Bill naming |
| `d6118b5` | Curse bubble on fish loss |
| `8082fb7` | Idle-reel hook-shake + predator steal |
| `eef9287` | Trophy case striped-bass.png |
| `0e64ba2` | User: trophy case image |
| `c384046` / `99b01fb` | Bill append tips |
| `7c9f18e` / `2cdea21` / `dc1d863` | Social cards |
| `0fbce6a` | Random Bill tips + 60% boost |
| `89ec377` | Storm/alien odds (**also regressed UFO to 50% — bug**) |
| `4247aa4` | Google Analytics |
| `d2fd72e` | Pass-along for prior UFO arc |

## Uncommitted work

None — worktree clean at `776c125`.

## Open threads

| Thread | State | Context | Done when |
|--------|-------|---------|-----------|
| Hook-shake feel | **needs playtest** | `surf-rats/index.html` — `FISH_IDLE_GRACE=3`, `FISH_IDLE_JUMP_CHANCE=0.12`, reset on space **release** | User confirms shakes happen when idle, not while burst-pumping |
| UFO spawn rate | stable at 5% | `UFO_CHANCE = 0.05` — user recalled ~10%; production choice was 5% in `0aa036c` | Only change if user asks |
| Scratch workspace drift | ongoing | `/home/rocky/surf-rats/` has no git; sync manually from `games/surf-rats/` | Edit in `/home/rocky/games/surf-rats/` instead |
| Map tooltips / SPOTS[] | deferred (prior session) | user said "better not perfect" | Revisit if polishing map |
| Full-trip playtest | deferred | schools, cast charge, snags, storms | Structured playtest pass |
| Deleted stray repo | closed | `jbdamask/surf-rats` created by mistake; user deleted | N/A |

**In scope this session and finished:** all user-requested features through loss-reason reporting.

## Recommended next — this session's view

Live-playtest hook-shake on the deployed game. The mechanic went through three iterations in one session (20%/sec on idle → grace + 6% → release-based timer + 12%) because burst pumping (press/**release** SPACE) was being misread as idle. If shakes still feel wrong, adjust `FISH_IDLE_GRACE` and `FISH_IDLE_JUMP_CHANCE` near ```734:736:surf-rats/index.html``` — don't revert to resetting on press.

If skipping playtest, the next polish item from prior pass-alongs is still map tooltips / SPOTS[] accuracy.

## Stated direction

User did not specify what comes after this session. They asked for `/pass-along` at end.

## Decisions and rules

- **Canonical repo:** `https://github.com/jbdamask/games` — game path `surf-rats/`, live URL `http://johndamask.com/games/surf-rats/`. Do not create a separate `surf-rats` repo.
- **Bill naming:** Only **Montauk Bill** is "Bill". The player is `G.player` with name from `PLAYER_NAMES` (GOOGIN, HERB, DANGLER, SPOTBURN).
- **UFO spawn:** `UFO_CHANCE = 0.05` per spot entry (not per trip). Once aliens visit a spot (`G.alienSpot`), no repeat at that spot same trip.
- **Bill tip boost:** Match spot + lure + color within 60 real seconds → 60% hook/sec (`BILL_TIP_BITE_CHANCE`).
- **Hook-shake:** Idle timer resets each time spacebar is **released**; accumulates only while spacebar is **up**.

## Traps

- **Deploy from wrong copy:** `89ec377` pulled stale `UFO_CHANCE = 0.5` from `/home/rocky/surf-rats/` standalone workspace. Always commit from `/home/rocky/games`.
- **Burst reeling ≠ holding SPACE:** Help text says pump in bursts. Penalizing `!keys.space` without release-based reset made hooks shake during normal technique.
- **`G.bill` was the player sprite** for a long time — only `G.rival` is Montauk Bill on the map. Don't reintroduce `G.bill` for the angler.
- **Multiple og:image tags:** Facebook uses first; Twitter uses `twitter:image`; LinkedIn uses first `og:image` (facebook card).
- **Git author on VM:** commits need `GIT_AUTHOR_*` env vars — no global git config on this machine.

## Waiting on a human

Nothing blocked.

## Housekeeping

- `/home/rocky/games` — canonical git clone, clean at `776c125`.
- `/home/rocky/surf-rats/` — scratch mirror, no `.git`; may be stale vs games repo.
- `/tmp/games-deploy` — ephemeral clone from earlier in session; ignore.
- Stray `jbdamask/surf-rats` GitHub repo deleted by user.
